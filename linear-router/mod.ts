/**
 * Linear Router - Default router implementation for Mage applications.
 *
 * Uses O(n) linear search for route matching. Simple, fast startup, and
 * perfect for serverless deployments with frequent cold starts.
 *
 * @module
 */

export { LinearRouter } from "./linear-router.ts";
